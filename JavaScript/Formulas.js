/*calcmagic(
        //how do i do this,,
        cauldron * 1 + study * 10 + feed * 100 + feedperson * 1000 + drink * 10000
      );
      //increase red
      calcred(
        // filter
        (((((redfilter * (redfilter * redupgrade1 + 1) * (1 + Math.sqrt(finerfilterscount) * 2) +
          // pointer
          redpointer *
            10 *
            (redpointer * redupgrade3 + 1) *
            (1 + Math.sqrt(focussedpointerscount) * 2) +
          // bigredfilter
          bigredfilter * 100 * (1 + Math.sqrt(finerfilterscount) * 2) +
          // bigredpointer
          bigredpointer * 1000 * (1 + Math.sqrt(focussedpointerscount) * 2)) *
          // rednanometerwave
          (rednanometerwave * 0.5 + 1) *
          (1 + Math.log1p(micrometerwavecount) * 1.5) *
          // weaksynergygreen
          (Math.log1p(colors.green) * greenupgrade2 + 1)) /
          (10 * greenupgrade2 + 1)) *
          // weaksynergyblue
          (Math.log1p(colors.blue) * blueupgrade2 + 1)) /
          (10 * blueupgrade2 + 1)) *
          // stronger synergy
          (1 + Math.sqrt(strongersynergycount) * 5) *
          // task booster
          taskBooster *
          // yellow larger prisms
          (1 + 2 * largerprismscount) *
          // yellow color harmony
          (1 +
            (Math.log1p(colors.yellow) / 10) *
              (colorharmonycount / Math.sqrt(colorharmonycount + 1))) *
          // tri-color boost
          (1 + tricolorboostcount * 0.5 * (colors.yellow / 1000)) *
          // black
          (blackboost + 1)
      );
      //increase green
      calcgreen(
        // filter
        (((((greenfilter *
          (greenfilter * greenupgrade1 + 1) *
          (1 + Math.sqrt(finerfilterscount) * 2) +
          // pointer
          greenpointer *
            10 *
            (greenpointer * greenupgrade3 + 1) *
            (1 + Math.sqrt(focussedpointerscount) * 2) +
          // biggreenfilter
          biggreenfilter * 100 * (1 + Math.sqrt(finerfilterscount) * 2) +
          // biggreenpointer
          biggreenpointer * 1000 * (1 + Math.sqrt(focussedpointerscount) * 2)) *
          // greennanometerwave
          (greennanometerwave * 0.5 + 1) *
          (1 + Math.log1p(micrometerwavecount) * 1.5) *
          // weaksynergyred
          (Math.log1p(colors.red) * redupgrade2 + 1)) /
          (10 * redupgrade2 + 1)) *
          // weaksynergyblue
          (Math.log1p(colors.blue) * blueupgrade2 + 1)) /
          (10 * blueupgrade2 + 1)) *
          // stronger synergy
          (1 + Math.sqrt(strongersynergycount) * 5) *
          // task booster
          taskBooster *
          // yellow larger prisms
          (1 + 2 * largerprismscount) *
          // yellow color harmony
          (1 +
            (Math.log1p(colors.yellow) / 10) *
              (colorharmonycount / Math.sqrt(colorharmonycount + 1))) *
          // tri-color boost
          (1 + tricolorboostcount * 0.5 * (colors.yellow / 1000)) *
          // black
          (blackboost + 1)
      );

      //increase blue
      calcblue(
        // filter
        (((((bluefilter * (bluefilter * blueupgrade1 + 1) * (1 + Math.sqrt(finerfilterscount) * 2) +
          // pointer
          bluepointer *
            10 *
            (bluepointer * blueupgrade3 + 1) *
            (1 + Math.sqrt(focussedpointerscount) * 2) +
          // bigbluefilter
          bigbluefilter * 100 * (1 + Math.sqrt(finerfilterscount) * 2) +
          // bigbluepointer
          bigbluepointer * 1000 * (1 + Math.sqrt(focussedpointerscount) * 2)) *
          // bluenanometerwave
          (bluenanometerwave * 0.5 + 1) *
          (1 + Math.log1p(micrometerwavecount) * 1.5) *
          // weaksynergygreen
          (Math.log1p(colors.green) * greenupgrade2 + 1)) /
          (10 * greenupgrade2 + 1)) *
          // weaksynergyred
          (Math.log1p(colors.red) * redupgrade2 + 1)) /
          (10 * redupgrade2 + 1)) *
          // stronger synergy
          (1 + Math.sqrt(strongersynergycount) * 5) *
          // task booster
          taskBooster *
          // yellow larger prisms
          (1 + 2 * largerprismscount) *
          // yellow color harmony
          (1 +
            (Math.log1p(colors.yellow) / 10) *
              (colorharmonycount / Math.sqrt(colorharmonycount + 1))) *
          // tri-color boost
          (1 + tricolorboostcount * 0.5 * (colors.yellow / 1000)) *
          // black
          (blackboost + 1)
      );*/
